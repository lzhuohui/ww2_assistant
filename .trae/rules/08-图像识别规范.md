---
name: 08-图像识别规范
description: 适用于截图处理和图像识别的规则
globs: ["core/vision/**/*.py", "**/screen.py", "**/template.py"]
---

# 图像识别规范

## 截图处理
- 统一使用RGB格式
- 截屏后及时释放内存
- 支持多分辨率（缩放或预生成模板）

```python
def screenshot_to_cv(adb):
    pil_img = adb.screenshot_pil()
    cv_img = cv2.cvtColor(np.array(pil_img), cv2.COLOR_RGB2BGR)
    pil_img.close()
    return cv_img
```

## 模板匹配
- 返回(坐标, 置信度)元组
- 置信度阈值可配置（默认0.8）
- 支持ROI区域识别

```python
def find_template(screenshot, template, threshold=0.8):
    result = cv2.matchTemplate(screenshot, template, cv2.TM_CCOEFF_NORMED)
    _, max_val, _, max_loc = cv2.minMaxLoc(result)
    if max_val >= threshold:
        return (max_loc[0] + template.shape[1]//2, 
                max_loc[1] + template.shape[0]//2, max_val)
    return None
```

## 异常处理
- 所有识别函数包含异常处理
- 失败时记录日志，提供降级策略

## 性能优化
- 控制识别频率（1-3秒/次）
- 缓存频繁识别结果
