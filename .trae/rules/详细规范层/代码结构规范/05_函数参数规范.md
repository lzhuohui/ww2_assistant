---
alwaysApply: false
description: "定义函数参数时使用，确保参数定义符合PEP 8规范且具有合理的默认值"
---
# 函数参数规范

## 参数默认值

必须有默认值：
```python
def create(
    title: str="测试标题",
    icon: str="HOME",
    enabled: bool=True,
) -> ft.Container:
```

### 核心原则
- 所有参数应有必要的默认数据
- None不显示，测试看不到，无法判定是否正确
- 默认值应能展示完整的功能效果

### 默认值来源规则

| 来源 | 适用场景 | 示例 |
|------|----------|------|
|直接硬编码|通用默认值，用户未指定| `title: str="测试标题"` |
|用户指定变量|用户明确指定的配置| `size: int=USER_TITLE_SIZE` |
|配置文件|通用配置数据| `配置.获取尺寸("字体","font_size_lg")` |

### 严格禁止
1. 禁止将默认值放在用户指定变量区域（用户明确指定除外）
2. 禁止使用DEFAULT_前缀的模块级变量作为函数默认值
3. 禁止在用户未指定时创建USER_变量

## 参数传递格式（PEP 8）

函数定义调用时，`=`两边不加空格：
```python
def create(title: str="默认值", size: int=16):
    pass

ContainerTitle.create(title=title, text_size=USER_TITLE_SIZE, padding=3)
```

## 参数命名规范

- 使用snake_case命名
- 参数名称应有描述性
- 所有参数应有类型注解
- 返回值应有类型注解

```python
from typing import Callable, Optional

def create(
    title: str="默认值",
    on_click: Optional[Callable]=None,
) -> ft.Container:
    pass
```
