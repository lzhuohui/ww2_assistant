---
alwaysApply: true
description: |
  模块架构规范：定义模块拆分原则、隔离约束和分类判断逻辑。
  在编程工作流阶段4中调用，用于确定模块类型和结构。
---

# 模块架构规范

## 一、模块拆分原则

### 1.1 单一职责原则
每个模块只负责一个明确的功能领域：
- 界面显示 → 脚本界面类
- 动作执行 → 游戏动作类
- 信息查询 → 游戏查询类
- 数据处理 → 数据操作类

### 1.2 接口隔离原则
模块之间通过清晰接口通信：
- 调用者不依赖不需要的接口
- 被调用者不暴露内部实现
- 接口参数明确，返回值清晰

### 1.3 依赖倒置原则
高层模块不依赖低层模块，二者都依赖抽象：
- 通过配置文件传递参数
- 通过回调函数传递控制
- 通过事件机制传递状态

---

## 二、模块分类判断

### 2.1 4类模块定义

| 分类 | 功能描述 | 技术栈 | 变量使用 | 代码结构 |
|------|----------|--------|----------|----------|
| **脚本界面类** | Flet可视化界面，用户交互 | flet | USER_变量为模块组成部分 | 《模块代码结构.md》三、脚本界面类 |
| **游戏动作类** | 游戏内动作执行，坐标操作 | pyautogui, time | 测试数据在if __name__内部 | 《模块代码结构.md》四、游戏动作类 |
| **游戏查询类** | 游戏信息查询，图像识别 | cv2, numpy, PIL | 测试数据在if __name__内部 | 《模块代码结构.md》五、游戏查询类 |
| **数据操作类** | 数据存储读取，配置管理 | json, os, pickle | 测试数据在if __name__内部 | 《模块代码结构.md》六、数据操作类 |

### 2.2 判断流程（AI必须执行）

**第一步：AI分析需求提取关键词**
从用户需求描述中识别关键词，用于匹配模块类型。

**第二步：按顺序匹配分类**：
1. 包含以下任一关键词 → **脚本界面类**
   - 界面、窗口、UI、显示、面板、控制台、可视化
   
2. 包含以下任一关键词 → **游戏动作类**
   - 点击、滑动、动作、执行、操作、移动、拖拽、按压
   
3. 包含以下任一关键词 → **游戏查询类**
   - 查询、识别、检测、查找、搜索、状态、位置、存在
   
4. 包含以下任一关键词 → **数据操作类**
   - 保存、读取、数据、配置、设置、记录、存储、加载

**第三步：默认规则**
- 无明确关键词 → 脚本界面类
- 多个关键词匹配 → 按上述顺序优先匹配

### 2.3 执行要求
1. 只匹配一个类型
2. 调用《模块代码结构.md》中对应模板
3. **变量使用规则**：
   - 脚本界面类：USER_变量为模块组成部分，必须生效，AI不得修改
   - 其他类型：测试数据在if __name__内部，与模块隔离

---

## 三、模块隔离约束

### 3.1 调用约束
1. **禁止直接修改被调用模块**：调用者不得修改被调用模块的内部实现
2. **修改被调用者需评估影响**：如必须修改，必须先评估对其他调用者的影响
3. **修改需用户同意**：影响评估后，必须获得用户明确同意方可继续

### 3.2 数据约束
1. **配置数据分离**：运行时常量使用USER_前缀变量
2. **运行时数据隔离**：模块间通过参数传递数据
3. **持久化数据独立**：文件操作使用独立数据文件

### 3.3 测试约束
1. **测试不干扰功能**：测试逻辑不得添加额外数据
2. **测试如实反映**：测试结果必须反映模块真实功能
3. **测试独立验证**：每个模块有独立的测试逻辑

---

## 四、在编程工作流中的调用

本规范在**编程工作流阶段4：按规则执行**中被调用：

```
阶段4执行流程：
1. 调用本规范判断模块类型（第2节）
2. 调用《模块代码结构.md》对应模板处理代码
3. 遵循《代码风格规范.md》格式要求
4. 遵循《安全规范.md》安全要求
```

## 五、版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| 2.3 | 2025-03-25 | 移除阶段4中的成果提交规范引用，移至阶段5 |
| 2.2 | 2025-03-25 | 同步章节引用（接口规范新增后章节号变更） |
| 2.1 | 2025-03-25 | 修复变量说明和章节引用，与模块代码结构一致 |
| 2.0 | 2025-03-25 | V2版本，加入模块分类判断逻辑 |
| 1.0 | 2025-03-24 | 初始版本，定义模块拆分原则 |
