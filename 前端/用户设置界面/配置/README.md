# 配置

## 职责

- 定义界面配置数据
- 定义主题配置
- 定义卡片配置
- 提供配置管理功能

## 依赖规则

- ✅ 可依赖：无（最底层）
- ❌ 禁止：依赖任何其他层

## 模块示例

| 模块 | 功能描述 |
|------|---------|
| 界面配置.py | 界面尺寸、颜色配置、主题管理 |
| 主题配置.py | 主题定义（浅色/深色） |
| 卡片配置.py | 卡片配置数据 |
| 配置管理器.py | 配置读写管理 |

## 设计原则

1. 配置应该是纯数据
2. 配置应该可序列化
3. 配置应该可热更新
4. 配置模块不应该包含UI逻辑
