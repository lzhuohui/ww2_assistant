# 数据接口隔离方案

## 方案背景

### 问题描述
在项目开发过程中，出现了调用者模块覆盖被调用模块用户指定数据的问题，违反了"AI被调保护规范"。

### 问题根源
1. 调用者模块通过外部接口获取被调用模块的用户指定数据
2. 调用者模块硬编码默认值，覆盖了被调用模块的默认值
3. 缺乏清晰的数据流转规则

## 方案目标

### 核心目标
确保调用者模块正确继承被调用模块内部的数据，而非通过外部接口获取。

### 具体目标
1. 建立清晰的数据流转规则
2. 增强数据流转的可控性
3. 提高系统的整体稳定性
4. 避免未来再次出现类似问题

## 方案内容

### 第1步：全面核查模块调用关系

#### 1.1 列出所有模块
- 页面层模块
- 组件层模块
- 零件层模块
- 配置层模块

#### 1.2 分析每个模块的import语句
- 识别每个模块调用的其他模块
- 建立模块调用关系图

#### 1.3 建立模块调用关系图
```
页面层
  ↓ 调用
组件层
  ↓ 调用
零件层
  ↓ 调用
配置层
```

### 第2步：识别用户指定数据

#### 2.1 搜索所有用户指定数据
- 搜索所有"用户指定变量 - AI不得修改"的注释
- 列出所有DEFAULT_WIDTH、DEFAULT_HEIGHT等变量

#### 2.2 标记数据所属模块
- 标记每个用户指定数据所属的模块
- 建立数据归属关系表

#### 2.3 建立数据归属关系表
| 数据名称 | 所属模块 | 默认值 | 说明 |
|---------|---------|-------|------|
| DEFAULT_WIDTH | 自定义下拉框.py | 50 | 下拉框默认宽度 |
| DEFAULT_HEIGHT | 自定义下拉框.py | 32 | 下拉框默认高度 |
| DEFAULT_WIDTH | 卡片容器.py | 800 | 卡片默认宽度 |
| DEFAULT_HEIGHT | 卡片容器.py | 100 | 卡片默认高度 |
| DEFAULT_WIDTH | 导航按钮.py | 200 | 导航按钮默认宽度 |
| DEFAULT_WIDTH | 椭圆开关.py | 60 | 椭圆开关默认宽度 |
| DEFAULT_HEIGHT | 椭圆开关.py | 26 | 椭圆开关默认高度 |

### 第3步：实施数据接口隔离

#### 3.1 检查调用者模块
- 检查调用者模块是否通过外部接口获取被调用模块的用户指定数据
- 检查调用者模块是否硬编码了被调用模块的默认值

#### 3.2 断开外部接口
- 断开调用者模块的外部数据获取接口
- 移除调用者模块中的硬编码默认值

#### 3.3 确保正确继承
- 确保调用者模块正确继承被调用模块内部的数据
- 如果用户指定了数据，通过配置文件传递

### 第4步：全面测试

#### 4.1 测试所有页面
- 测试系统设置页面
- 测试策略设置页面
- 测试其他页面

#### 4.2 测试所有控件
- 测试下拉框控件
- 测试输入框控件
- 测试开关控件
- 测试其他控件

#### 4.3 确保功能正常
- 确保所有功能正常工作
- 确保没有引入新的问题

## 实施案例

### 案例：下拉框宽度问题

#### 当前问题
- 调用者模块（控件工厂）通过外部接口获取宽度参数
- 调用者模块（开关下拉卡片）硬编码默认宽度
- 被调用模块（自定义下拉框）有用户指定的默认宽度50

#### 解决方案
1. 断开控件工厂的外部宽度参数接口
2. 移除开关下拉卡片中的硬编码默认宽度
3. 让自定义下拉框使用自己的默认宽度50
4. 如果用户指定了宽度，通过配置文件传递

#### 实施步骤
1. 修改控件工厂._create_dropdown方法，不传递width参数
2. 修改控件工厂._create_switch_dropdown_controls方法，不传递width参数
3. 修改开关下拉卡片.create方法，不设置默认宽度
4. 测试所有下拉框控件

## 预期效果

### 架构层面
1. 建立清晰的数据流转规则
2. 增强数据流转的可控性
3. 提高系统的整体稳定性

### 代码层面
1. 避免重复定义
2. 代码更清晰
3. 维护更简单

### 规则层面
1. 符合"AI被调保护规范"
2. 不覆盖被调用模块的变量
3. 让被调用模块使用自己的默认值

## 风险评估

### 潜在风险
1. 修改接口可能影响现有功能
2. 需要全面测试
3. 工作量较大

### 应对措施
1. 制定详细的实施计划
2. 逐步实施，每一步都测试
3. 确保代码稳定性

## 实施计划

### 第1阶段：核查与识别
1. 全面核查模块调用关系
2. 识别所有用户指定数据
3. 建立模块调用关系图和数据归属关系表

### 第2阶段：实施隔离
1. 检查调用者模块
2. 断开外部接口
3. 确保正确继承

### 第3阶段：全面测试
1. 测试所有页面
2. 测试所有控件
3. 确保功能正常

### 第4阶段：文档更新
1. 更新架构文档
2. 更新开发规范
3. 更新自检清单

## 总结

本方案通过全面核查模块调用关系、识别用户指定数据、实施数据接口隔离，从架构层面解决调用者模块覆盖被调用模块用户指定数据的问题，建立清晰的数据流转规则，增强数据流转的可控性，提高系统的整体稳定性。
