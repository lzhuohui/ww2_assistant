# 修改记录

## 记录格式
- **日期时间**：YYYY-MM-DD HH:MM
- **修改文件**：文件路径
- **修改类型**：新增/修改/删除
- **修改内容**：具体修改内容
- **修改原因**：为什么需要修改
- **影响范围**：影响哪些模块
- **验证结果**：是否通过测试

---

## 2026-03-11 修改记录

### 修改1：标签下拉框.py - 移除默认值重新定义
- **日期时间**：2026-03-11
- **修改文件**：`零件层/标签下拉框.py`
- **修改类型**：修改
- **修改内容**：
  - 移除 `ui_config = config.定义尺寸.get("组件", {})`
  - 移除 `default_width = ui_config.get("dropdown_width", 120)`
  - 移除 `current_width = width if width is not None else default_width`
  - 直接传递 `width` 参数给 `CustomDropDown.create()`
- **修改原因**：违反"只调用不覆盖"原则，不应该重新定义被调用模块的默认值
- **影响范围**：标签下拉框.py，不影响其他模块
- **验证结果**：✅ 通过测试

### 修改2：通用卡片.py - 移除对图标标题的布局干预
- **日期时间**：2026-03-11
- **修改文件**：`组件层/通用卡片.py`
- **修改类型**：修改
- **修改内容**：
  - 移除 `left_container` 中的 `top=0, bottom=0` 属性
  - 移除 `alignment=ft.Alignment(0, 0)` 属性
  - 保留 `left=card_padding` 和 `top=10`
- **修改原因**：违反"只装配不干预"原则，不应该干预被调用模块的内部布局
- **影响范围**：通用卡片.py，不影响其他模块
- **验证结果**：✅ 通过测试

### 修改3：图标标题.py - 集成所有元素
- **日期时间**：2026-03-11
- **修改文件**：`零件层/图标标题.py`
- **修改类型**：修改
- **修改内容**：
  - 集成图标、主标题、帮助标签、分割线、副标题
  - 实现主标题和副标题的底部对齐
  - 帮助标签定位在分割线的右上角
- **修改原因**：用户需求，将所有元素集成到一个组件中
- **影响范围**：图标标题.py，通用卡片.py需要适配
- **验证结果**：✅ 通过测试

### 修改4：通用卡片.py - 简化左侧布局
- **日期时间**：2026-03-11
- **修改文件**：`组件层/通用卡片.py`
- **修改类型**：修改
- **修改内容**：
  - 移除对帮助标签、分割线的直接创建
  - 移除 `multirow_config` 相关变量
  - 移除 `left_width` 变量
  - 直接调用 `IconTitle.create()` 并传递所有参数
- **修改原因**：图标标题已经集成了所有元素，通用卡片只需要调用
- **影响范围**：通用卡片.py，不影响其他模块
- **验证结果**：✅ 通过测试

### 修改5：通用卡片.py - 调试逻辑使用自定义控件
- **日期时间**：2026-03-11
- **修改文件**：`组件层/通用卡片.py`
- **修改类型**：修改
- **修改内容**：
  - 调试逻辑中导入 `LabelDropdown` 和 `LabelInput`
  - 使用自定义控件替代原生的 `ft.Dropdown` 和 `ft.TextField`
- **修改原因**：符合模块化设计原则，应该使用自定义控件而不是原生控件
- **影响范围**：通用卡片.py的调试逻辑，不影响其他模块
- **验证结果**：✅ 通过测试

### 修改6：自定义下拉框.py - 使用用户指定变量
- **日期时间**：2026-03-11
- **修改文件**：`零件层/自定义下拉框.py`
- **修改类型**：修改
- **修改内容**：
  - 在"用户指定变量"区域添加 `DEFAULT_WIDTH = 100` 和 `DEFAULT_HEIGHT = 32`
  - 移除从配置文件获取默认值的代码
  - 使用用户指定变量作为默认值
- **修改原因**：用户要求在"用户指定变量"区域定义宽高数据
- **影响范围**：自定义下拉框.py，不影响其他模块
- **验证结果**：✅ 通过测试

### 修改7：批量修正零件层模块 - 添加用户指定变量
- **日期时间**：2026-03-11
- **修改文件**：`零件层/卡片容器.py`, `零件层/分割线.py`, `零件层/导航按钮.py`, `零件层/图标标题.py`
- **修改类型**：修改
- **修改内容**：
  - 卡片容器.py：添加阴影参数、动画参数、边框参数
  - 分割线.py：添加 `DIVIDER_WIDTH`, `DIVIDER_HEIGHT`, `DIVIDER_OPACITY`, `DIVIDER_BLUR`
  - 导航按钮.py：添加 `DEFAULT_WIDTH`
  - 图标标题.py：添加 `DEFAULT_ICON_SIZE`, `DEFAULT_TITLE_SIZE`
- **修改原因**：遵循"非用户指定变量，调用者不得更改被调用者的数据"规范
- **影响范围**：所有零件层模块，不影响其他模块
- **验证结果**：✅ 通过测试

---

## 违规记录

### 违规1：标签下拉框重新定义默认值
- **发现时间**：2026-03-11
- **违规文件**：`零件层/标签下拉框.py`
- **违规内容**：重新定义了 `default_width`，覆盖了自定义下拉框的默认值
- **违规类型**：违反"只调用不覆盖"原则
- **修正措施**：移除默认值重新定义，直接传递参数
- **修正时间**：2026-03-11
- **修正状态**：✅ 已修正

### 违规2：通用卡片干预图标标题布局
- **发现时间**：2026-03-11
- **违规文件**：`组件层/通用卡片.py`
- **违规内容**：使用 `top=0, bottom=0, alignment=ft.Alignment(0, 0)` 强制干预图标标题的布局
- **违规类型**：违反"只装配不干预"原则
- **修正措施**：移除布局干预属性，简单放置图标标题
- **修正时间**：2026-03-11
- **修正状态**：✅ 已修正

---

## 待检查项

- [x] 检查所有组合零件是否重新定义了被调用模块的默认值
- [x] 检查所有组件层是否干预了被调用模块的内部布局
- [x] 检查是否有直接修改零件层核心模块的情况
- [x] 更新模块依赖关系图

---

## 系统性解决方案总结

### 1. 建立了模块依赖关系图
- 文件：`模块依赖关系.md`
- 内容：明确了零件层、组件层的模块分类和修改权限
- 作用：为所有修改提供明确的权限边界

### 2. 建立了修改记录机制
- 文件：`修改记录.md`
- 内容：记录所有修改的时间、内容、原因和影响范围
- 作用：防止修改历史丢失，避免重复犯错

### 3. 创建了自动检查脚本
- 文件：`违规检查.py`
- 功能：自动检测模块化设计违规
- 作用：在每次修改后自动检查，及时发现问题

### 4. 修正了所有违规
- 标签下拉框.py：移除了从配置文件重新定义默认值的代码
- 标签输入框.py：移除了从配置文件重新定义默认值的代码
- 通用卡片.py：移除了对图标标题布局的干预

### 5. 建立了违规检测清单
在修改任何代码前，必须检查：
1. 我是否正在修改零件层核心模块？（如果是，立即停止）
2. 我是否在组合零件中重新定义了被调用模块的默认值？（如果是，立即停止）
3. 我是否在组件层中干预了被调用模块的内部布局？（如果是，立即停止）
4. 我是否查看了当前模块的所有import语句？（必须查看）
5. 我是否确认了所有被调用模块的修改权限？（必须确认）

---

## 2026-03-12 经验/教训记录

### 经验1：点击禁用卡片功能的实现

#### 问题描述
需要实现点击图标标题后，整个卡片变暗，控件不可操作的功能。

#### 失败方案

**方案1：遮罩层方案**
```python
overlay = ft.Container(
    bgcolor="#00000000",
    expand=True,
    opacity=0 if current_enabled else 1,
    on_click=lambda e: None,
)
```
- **问题**：遮罩层覆盖所有内容，包括图标标题，导致图标标题也不能点击
- **教训**：遮罩层方案不适合需要选择性排除某些区域的场景

**方案2：设置控件的enabled属性**
```python
for control in controls:
    if hasattr(control, 'enabled'):
        control.enabled = new_enabled
```
- **问题**：不同控件类型的enabled实现不一致，ft.Row没有enabled属性
- **教训**：不能假设所有控件都有相同的属性

#### 成功方案

**方案3：参考主题设置卡片的实现**
```python
# 1. 设置透明度（视觉变暗）
control.opacity = 1.0 if new_enabled else 0.4

# 2. 调用控件的set_state方法（禁用操作）
if hasattr(control, 'set_state'):
    control.set_state(new_enabled)
```
- **成功原因**：
  - 透明度是所有控件都有的属性
  - 通过调用控件自己的set_state方法，让控件自己处理禁用逻辑
- **教训**：参考已有的成功实现，避免重复造轮子

#### 遇到的坑

**坑1：闭包问题**
```python
# 错误：set_enabled定义时，control还不存在
def set_enabled(new_enabled: bool):
    control.disabled = not new_enabled  # NameError
```
- **教训**：注意Python闭包中变量引用的时机

**坑2：PopupMenuButton的disabled属性需要update**
```python
control.disabled = not new_enabled
if control.page:
    control.update()  # 必须调用update才能更新UI
```
- **教训**：修改控件属性后需要调用update()方法

**坑3：通用卡片没有调用控件的set_state方法**
```python
# 错误：只设置了透明度
control.opacity = 1.0 if new_enabled else 0.4

# 正确：还需要调用set_state方法
if hasattr(control, 'set_state'):
    control.set_state(new_enabled)
```
- **教训**：视觉变化和功能禁用需要分开处理

#### 设计原则总结

1. **模块化设计**：每个控件自己负责自己的禁用逻辑
2. **通用性**：使用透明度（所有控件都有）+ set_state方法（可选）
3. **封装性**：通用卡片不需要知道控件内部实现细节
4. **可扩展性**：新控件只需要实现set_state方法即可支持禁用功能

#### 影响文件
- `零件层/自定义下拉框.py`：添加enabled参数和set_state方法
- `零件层/标签下拉框.py`：暴露set_state方法
- `组件层/通用卡片.py`：在sync_parts_state中调用控件的set_state方法

#### 验证结果
✅ 通过测试
